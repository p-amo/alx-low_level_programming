#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int mai(void)

{
	char c;

	c = 'a';
	while
		(c <= 'a') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
